import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  // The demo store writes to the local filesystem; nothing here needs bundling changes.
  experimental: {},
};

export default nextConfig;
